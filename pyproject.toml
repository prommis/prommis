[build-system]
requires = [
    "setuptools >= 64",
    "setuptools_scm >= 7",
]
build-backend = "setuptools.build_meta"

[project]
name = "prommis"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "idaes-pse",
    "pyomo",
]

[tool.setuptools_scm]